@model SistemaContable.Models.ViewModels.ItemViewModel

<div class="tab-pane fade" id="tab-compras" role="tabpanel">
    <div class="row mb-4">
        <div class="col-md-6">
            <label class="form-label" for="UnidadMedidaInventarioId">Unidad de Medida (Inventario) *</label>
            <div class="input-group">
                <select class="form-select select2-um" required data-val="true" data-val-required="La unidad de medida de inventario es requerida" id="UnidadMedidaInventarioId" name="UnidadMedidaInventarioId">
                    <option value="">Seleccione una unidad</option>
                </select>
                <button type="button" class="btn btn-outline-primary" id="btnNuevaUnidad">
                    <i class="fas fa-plus"></i>
                </button>
            </div>
            <span class="text-danger field-validation-valid" data-valmsg-for="UnidadMedidaInventarioId" data-valmsg-replace="true"></span>
        </div>
        <div class="col-md-6">
            <label for="UnidadCompraId" class="form-label">Unidad de Compra Preferida</label>
            <select id="UnidadCompraId" name="UnidadCompraId" class="form-select" disabled>
                <option value="">Debe agregar contenedores primero</option>
            </select>
            <small class="text-muted">Se habilitará después de configurar los contenedores</small>
        </div>
    </div>
    
    <!-- Unidades y Conversiones -->
    <div class="card mb-4">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h5 class="card-title mb-0">Conversiones de Unidades</h5>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered table-hover" id="tablaContenedores">
                    <thead class="bg-light">
                        <tr>
                            <th style="width: 50px;">No</th>
                            <th>Contenedor</th>
                            <th>Etiqueta</th>
                            <th>Cantidad</th>
                            <th>Costo</th>
                            <th style="width: 50px;"></th>
                        </tr>
                    </thead>
                    <tbody>
                        <!-- Las filas se agregarán dinámicamente -->
                    </tbody>
                </table>
            </div>
            <div class="mt-2">
                <button type="button" id="btnAgregarFila" class="btn btn-primary btn-sm">
                    <i class="fas fa-plus"></i> Agregar Fila
                </button>
            </div>
        </div>
    </div>
    
    <div class="card mb-4">
        <div class="card-header">
            <h5 class="card-title mb-0">Proveedores</h5>
        </div>
        <div class="card-body">
            <p>Esta sección está en desarrollo.</p>
        </div>
    </div>
</div>

<!-- Contenedor oculto para IDs eliminados -->
<div id="conversiones-eliminadas" style="display:none;"></div>